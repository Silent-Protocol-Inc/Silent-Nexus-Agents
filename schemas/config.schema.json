{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "ActivityConfig": {
      "additionalProperties": false,
      "description": "Controls the three-layer activity timeline. The improved defaults need no configuration; these only exist to override them.",
      "properties": {
        "animation": {
          "default": "nexus",
          "description": "Processing animation style: `nexus`, `minimal`, or `off`.",
          "type": "string"
        },
        "animation_speed": {
          "default": "normal",
          "description": "Animation cadence: `slow`, `normal`, or `fast`.",
          "type": "string"
        },
        "coalesce_events": {
          "default": true,
          "description": "Coalesce related events (tool start/complete, retries) into one entry.",
          "type": "boolean"
        },
        "expand_shortcut": {
          "default": "ctrl+e",
          "description": "Shortcut that toggles the expanded activity detail.",
          "type": "string"
        },
        "mode": {
          "default": "default",
          "description": "Timeline verbosity: `default`, `detailed`, or `debug`.",
          "type": "string"
        },
        "reasoning_preview_lines": {
          "default": 3,
          "description": "Max wrapped lines in the NEXUS processing preview (1–10).",
          "format": "uint8",
          "minimum": 0.0,
          "type": "integer"
        },
        "reduced_motion": {
          "default": false,
          "description": "Force reduced motion regardless of the terminal/global setting.",
          "type": "boolean"
        },
        "show_diagnostics": {
          "default": false,
          "description": "Show diagnostic-only events in the main timeline without debug mode.",
          "type": "boolean"
        },
        "show_token_usage": {
          "default": false,
          "description": "Show token usage in progress/completion summaries.",
          "type": "boolean"
        },
        "tool_icons": {
          "default": "geometric",
          "description": "Marks drawn beside tool rows: `geometric` (default), `emoji`, `ascii`.\n\nGeometric is the default because it is single-width in every terminal and needs no emoji font. `emoji` is opt-in: those marks are two cells wide and render as boxes on several mobile clients. A terminal that cannot draw Unicode at all falls back to `ascii` whatever this says.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "CompactionConfig": {
      "additionalProperties": false,
      "description": "Folding a turn's own history so it can keep going.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Turning this off restores the pre-2.10 behaviour: a turn that outgrows its window stops instead of folding.",
          "type": "boolean"
        },
        "trigger_ratio": {
          "default": 0.75,
          "description": "Fraction of the prompt budget that triggers a fold, 0.1–0.95. Below the context compiler's own drop threshold so this runs first and nothing is discarded silently.",
          "format": "float",
          "type": "number"
        }
      },
      "type": "object"
    },
    "GeneralConfig": {
      "additionalProperties": false,
      "properties": {
        "default_agent": {
          "default": "nexus",
          "description": "Default agent profile used for plain `snx run`.",
          "type": "string"
        },
        "no_color": {
          "default": false,
          "description": "Disable all color output (also honors NO_COLOR).",
          "type": "boolean"
        },
        "reduced_motion": {
          "default": false,
          "description": "Disable animations (also honors reduced-motion preferences).",
          "type": "boolean"
        },
        "test_command": {
          "default": null,
          "description": "Command run by `/test` and `snx test` (e.g. `cargo test`). Unset means the test command is honestly reported as not configured.",
          "type": [
            "string",
            "null"
          ]
        },
        "theme": {
          "default": "nexus-dark",
          "description": "UI theme name (`nexus-dark`, `no-color`).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "LimitMode": {
      "description": "Ownership of model token limits. Missing fields deserialize as `manual`, preserving every existing 1.x configuration verbatim.",
      "enum": [
        "auto",
        "manual"
      ],
      "type": "string"
    },
    "LimitSource": {
      "description": "Origin of an effective model limit. These values are safe to persist and display; they contain no endpoint or credential data.",
      "enum": [
        "provider_metadata",
        "bundled_catalog",
        "configured_conservative"
      ],
      "type": "string"
    },
    "LimitsConfig": {
      "additionalProperties": false,
      "properties": {
        "completion_reserve_tokens": {
          "default": 1024,
          "description": "Tokens reserved for the model completion when packing context.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "context_compaction": {
          "allOf": [
            {
              "$ref": "#/definitions/CompactionConfig"
            }
          ],
          "default": {
            "enabled": true,
            "trigger_ratio": 0.75
          },
          "description": "When a turn folds its own history to keep going."
        },
        "goal_runtime_budget_min": {
          "default": 120,
          "description": "Default per-goal wall-clock budget in minutes.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "goal_step_budget": {
          "default": 200,
          "description": "Default per-goal step budget.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "local_runaway_guard": {
          "allOf": [
            {
              "$ref": "#/definitions/RunawayGuardConfig"
            }
          ],
          "default": {
            "enabled": true,
            "max_identical_tool_repeats": 3,
            "max_no_progress_cycles": 3,
            "max_weighted_tokens": null
          },
          "description": "Protection against an agent that has stopped getting anywhere."
        },
        "max_cost_micros_per_turn": {
          "default": 0,
          "description": "Provider-reported monetary ceiling in micro-units per turn. Zero disables cost enforcement; non-zero fails closed for adapters that do not expose monetary usage.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_failures_per_turn": {
          "default": 6,
          "description": "Maximum recoverable failures before the turn stops.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_memory_writes_per_turn": {
          "default": 8,
          "description": "Maximum durable memory writes initiated by one turn.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_model_calls_per_turn": {
          "default": 24,
          "description": "Maximum provider requests in one foreground turn.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_recursion_depth": {
          "default": 2,
          "description": "Maximum delegation ancestry depth.",
          "format": "uint8",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_repeated_calls": {
          "default": 3,
          "description": "Maximum identical tool calls before loop detection stops the turn.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_retries": {
          "default": 3,
          "description": "Maximum consecutive failed/invalid model actions before stopping.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_steps_per_turn": {
          "default": 24,
          "description": "Maximum agent-loop steps per turn.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_subagents_per_run": {
          "default": 8,
          "description": "Maximum subagents created by one root run.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_tokens_per_turn": {
          "default": 250000,
          "description": "Aggregate input and output token ceiling per turn.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_tool_calls_per_turn": {
          "default": 48,
          "description": "Maximum tool executions in one foreground turn.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_turn_runtime_min": {
          "default": 30,
          "description": "Foreground turn wall-clock ceiling in minutes.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "retry": {
          "allOf": [
            {
              "$ref": "#/definitions/RetryConfig"
            }
          ],
          "default": {
            "max_attempts": 3,
            "max_wait_seconds": 120
          },
          "description": "How long to sit out a provider's rate limit before giving up on it."
        },
        "self_hosted_context_window": {
          "default": 32768,
          "description": "Context window given to a self-hosted model whose provider reports only an architecture maximum. Discovery caps the auto-derived window at this value — the reported maximum is a capability, not a size worth allocating on every request — so an entry with `limit_mode = \"auto\"` settles at `min(context_ceiling, this)` on every refresh, up or down. Raise it once here instead of per model. To pin one model to something else, set its `context_window` and `limit_mode = \"manual\"`, which takes the entry out of discovery's hands entirely.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "self_hosted_max_tokens_per_turn": {
          "default": 5000000,
          "description": "Aggregate token ceiling for a turn routed to a self-hosted provider (`ollama`, `llamacpp`). Those tokens are not metered, so the ceiling above — which exists to bound spend — is the wrong guard for them.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "McpServerConfig": {
      "additionalProperties": false,
      "properties": {
        "args": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "command": {
          "default": "",
          "description": "Command to launch (stdio transport).",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enabled state.",
          "type": "boolean"
        },
        "env_allowlist": {
          "default": [
            "PATH",
            "HOME",
            "LANG"
          ],
          "description": "Environment variables forwarded to the server process.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout_secs": {
          "default": 30,
          "description": "Startup/request timeout seconds.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "transport": {
          "default": "stdio",
          "description": "Transport: `stdio` or `http`.",
          "type": "string"
        },
        "trust": {
          "default": "untrusted",
          "description": "Trust level: `untrusted` (tools require approval) or `trusted`.",
          "type": "string"
        },
        "url": {
          "default": null,
          "description": "URL (http transport).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "MemoryConfig": {
      "additionalProperties": false,
      "properties": {
        "default_ttl_days": {
          "default": 90,
          "description": "Days after which unverified memories expire (0 = never).",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "enabled": {
          "default": true,
          "description": "Enable persistent memory.",
          "type": "boolean"
        },
        "global_enabled": {
          "default": false,
          "description": "Allow global (cross-project) memory. Off by default.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ModelConfig": {
      "additionalProperties": false,
      "properties": {
        "api_key_env": {
          "default": null,
          "description": "Environment variable holding the API key, if any. The value itself is never written to config or logs.",
          "type": [
            "string",
            "null"
          ]
        },
        "api_key_ref": {
          "default": null,
          "description": "Name of a credential-store entry holding the API key (managed by `snx auth` / the `/login` flow). The secret lives in the restricted credential store, never in this file.",
          "type": [
            "string",
            "null"
          ]
        },
        "auth": {
          "default": null,
          "description": "Credential source when not using `api_key_env`. Currently `codex` reuses the OpenAI Codex CLI's OAuth session (`~/.codex/auth.json`), so the device/browser login is performed by the trusted `codex` CLI and Silent Nexus consumes the resulting token. `None`/empty = use `api_key_env`.",
          "type": [
            "string",
            "null"
          ]
        },
        "base_url": {
          "default": "http://127.0.0.1:8080/v1",
          "description": "Base URL of the endpoint, e.g. `http://127.0.0.1:8080/v1`.",
          "type": "string"
        },
        "context_ceiling": {
          "default": null,
          "description": "Largest context the provider says the model architecture supports. Informational, and the ceiling `context_window` may be raised to.",
          "format": "uint",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "context_limit_source": {
          "allOf": [
            {
              "$ref": "#/definitions/LimitSource"
            }
          ],
          "default": "configured_conservative",
          "description": "Provenance for the effective cached context limit."
        },
        "context_window": {
          "default": 8192,
          "description": "Context window in tokens (prompt + completion). Self-hosted servers allocate a KV cache this large before answering, so this is a runtime cost, not a capability claim — see `context_ceiling`.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "first_token_timeout_secs": {
          "default": null,
          "description": "Seconds allowed for the *first* token. A self-hosted server has to load the model and run prefill before it can emit anything, which is not the same thing as a stalled stream. `None` uses the provider default: [`SELF_HOSTED_FIRST_TOKEN_SECS`] for `ollama`/`llamacpp`, otherwise `timeout_secs`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "keep_alive": {
          "default": null,
          "description": "How long Ollama keeps the model resident after a request (its `keep_alive`). `None` sends [`OLLAMA_DEFAULT_KEEP_ALIVE`] so the next turn does not pay another cold load. Ignored by other providers.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit_mode": {
          "allOf": [
            {
              "$ref": "#/definitions/LimitMode"
            }
          ],
          "default": "manual",
          "description": "Whether limits are operator-owned or refreshed from provider metadata."
        },
        "max_output_tokens": {
          "default": 1024,
          "description": "Maximum completion tokens per request.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "model": {
          "default": "",
          "description": "Model identifier passed to the provider.",
          "type": "string"
        },
        "native_tool_calls": {
          "default": null,
          "description": "Override native tool-calling support detection. When `false`, the structured-action compatibility layer is used.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "output_limit_source": {
          "allOf": [
            {
              "$ref": "#/definitions/LimitSource"
            }
          ],
          "default": "configured_conservative",
          "description": "Provenance for the effective cached output limit."
        },
        "prompt_cache": {
          "default": null,
          "description": "Ask the provider to cache the stable prefix of each request.\n\n`None` means on for metered providers: every turn re-sends the whole conversation, so a turn with two or more model calls always repays the one-time write premium. Set `false` to send exactly what earlier versions sent. Self-hosted providers ignore this — Ollama has no knob, and its KV reuse is governed by `keep_alive` instead.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "prompt_cache_ttl": {
          "default": null,
          "description": "How long a cached prefix stays warm: `\"5m\"` (default) or `\"1h\"`.\n\nOnly the Anthropic family takes a lifetime; the Responses backend manages its own. The longer window survives gaps between turns but the write costs more, so it needs more reuse to pay for itself.",
          "type": [
            "string",
            "null"
          ]
        },
        "provider": {
          "default": "llamacpp",
          "description": "Provider kind: `llamacpp`, `ollama`, `openai_compatible`, `claude-plan`, `anthropic`, `custom_http`, or `mock`.",
          "type": "string"
        },
        "reasoning_effort": {
          "default": null,
          "description": "Reasoning effort for models that honor it (`low`, `medium`, `high`, …). Used by the `codex` provider; ignored elsewhere.",
          "type": [
            "string",
            "null"
          ]
        },
        "role": {
          "default": "executor",
          "description": "Role hint for routing: `router`, `executor`, `planner`, `verifier`, `embedder`.",
          "type": "string"
        },
        "temperature": {
          "default": null,
          "description": "Sampling temperature.",
          "format": "float",
          "type": [
            "number",
            "null"
          ]
        },
        "timeout_secs": {
          "default": 120,
          "description": "Stall timeout in seconds: how long a stream may go silent between chunks, and the total deadline for a non-streaming request to a metered provider.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "tls_verify": {
          "default": true,
          "description": "Verify TLS certificates for remote endpoints. Disabling this is an explicit advanced setting and never the default.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "PolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "allowed_commands": {
          "default": [],
          "description": "Command prefixes allowed without prompting (e.g. `cargo check`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "commands": {
          "default": "ask",
          "description": "Decision for shell commands not otherwise classified.",
          "type": "string"
        },
        "denied_commands": {
          "default": [
            "sudo",
            "su",
            "doas",
            "shutdown",
            "reboot",
            "mkfs",
            "dd"
          ],
          "description": "Command prefixes always denied (e.g. `sudo`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "denied_paths": {
          "default": [],
          "description": "Additional denied path globs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "destructive": {
          "default": "ask",
          "description": "Decision for destructive operations (delete, reset). `deny` or `ask` only.",
          "type": "string"
        },
        "downloads": {
          "default": "ask",
          "description": "Decision for file downloads.",
          "type": "string"
        },
        "external": {
          "default": "ask",
          "description": "Decision for external side effects (git push, publish). `deny` or `ask` only.",
          "type": "string"
        },
        "network": {
          "default": "allow",
          "description": "Decision for internet search/fetch.",
          "type": "string"
        },
        "read_formats": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Per-format workspace read decisions. Workspace config naturally overlays global keys through the schema-v1 layered TOML merge.",
          "type": "object"
        },
        "reads": {
          "default": "allow",
          "description": "Decision for workspace reads: allow | ask | deny.",
          "type": "string"
        },
        "writes": {
          "default": "ask",
          "description": "Decision for workspace writes.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "RetryConfig": {
      "additionalProperties": false,
      "description": "Waiting out a provider quota, within bounds.\n\nThe bounds are the point. A window that resets in twenty seconds is worth waiting for; one that resets in six hours is not something to block a terminal on, and neither is an unbounded sequence of short waits.",
      "properties": {
        "max_attempts": {
          "default": 3,
          "description": "Rate-limit waits one turn will sit out before pausing instead.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_wait_seconds": {
          "default": 120,
          "description": "Longest single wait. A reset further out than this pauses the run so the operator decides, rather than the terminal blocking on it.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "RoutingConfig": {
      "additionalProperties": false,
      "properties": {
        "coding": {
          "default": null,
          "description": "Model name for coding tasks.",
          "type": [
            "string",
            "null"
          ]
        },
        "fallback": {
          "default": null,
          "description": "Fallback when a routed model is unavailable.",
          "type": [
            "string",
            "null"
          ]
        },
        "planning": {
          "default": null,
          "description": "Model name for planning.",
          "type": [
            "string",
            "null"
          ]
        },
        "simple": {
          "default": null,
          "description": "Model name for trivial classification/routing turns.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RunawayGuardConfig": {
      "additionalProperties": false,
      "description": "Local protection against a loop that is no longer making progress.\n\nDistinct from the provider's quota and from the model's context window, which are not ours to set. This is the harness deciding that continuing has stopped being useful — so it is measured in *progress*, and only secondarily in tokens.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Turning this off leaves `max_tokens_per_turn` as the only backstop.",
          "type": "boolean"
        },
        "max_identical_tool_repeats": {
          "default": 3,
          "description": "Repeats of one identical call before the guard fires.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_no_progress_cycles": {
          "default": 3,
          "description": "Consecutive cycles that change nothing before the guard fires.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_weighted_tokens": {
          "default": null,
          "description": "Weighted token ceiling for one turn. Unset inherits `max_tokens_per_turn`, so an existing configuration keeps its number.\n\nWeighted, not raw: input served from a provider cache bills at roughly a tenth of the full rate, and counting it whole made a warm turn look as expensive as a cold one. What the context gauge reports is unaffected — that still shows the true size of the prompt.",
          "format": "uint",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "SandboxConfig": {
      "additionalProperties": false,
      "properties": {
        "backend": {
          "default": "auto",
          "description": "Preferred backend: `auto`, `process`, `container`, `none`. `auto` picks the strongest available backend.",
          "type": "string"
        },
        "container_image": {
          "default": "debian:bookworm-slim@sha256:7b140f374b289a7c2befc338f42ebe6441b7ea838a042bbd5acbfca6ec875818",
          "description": "Container image used by the container backend.",
          "type": "string"
        },
        "cpu_limit_secs": {
          "default": 120,
          "description": "CPU seconds limit per execution.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "env_allowlist": {
          "default": [
            "PATH",
            "LANG",
            "TERM"
          ],
          "description": "Environment variables forwarded into sandboxed processes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_output_bytes": {
          "default": 200000,
          "description": "Maximum captured output bytes before truncation to artifact.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "memory_limit_mb": {
          "default": 1024,
          "description": "Memory limit in megabytes.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "network": {
          "default": "off",
          "description": "Network mode inside the sandbox: `off`, `restricted`, `full`.",
          "type": "string"
        },
        "timeout_secs": {
          "default": 120,
          "description": "Wall-clock timeout per execution in seconds.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "SelfImprovementConfig": {
      "additionalProperties": false,
      "description": "Recursive Self-Improvement (RSI) — the flagship `nexus` agent's ability to learn from finished turns. After a turn completes, the harness mines it for reusable workflows, repeated tool failures, and stated preferences and records them as *approval-gated* proposals (reviewed with `snx profile`). Nothing is ever applied without explicit operator approval; disabling this only stops the analysis, it changes no safety guarantee.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Analyze finished turns and record improvement proposals. On by default; set `false` to disable post-turn analysis entirely.",
          "type": "boolean"
        },
        "surface_pending": {
          "default": true,
          "description": "Surface the pending-proposal count in `snx status` and TUI startup so the operator knows there is a review queue waiting.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ThinkingConfig": {
      "additionalProperties": false,
      "description": "How much *optional* deliberation the harness performs, and whether the live activity component renders.\n\nThis block owns behavior only. Presentation of the activity component — preview line count, expand shortcut, animation, reduced motion — lives in [`ActivityConfig`] (`[tui.activity]`) and is deliberately not duplicated here, so there is exactly one source of truth per knob.\n\nNote that `[thinking].mode` and `[tui.activity].mode` are **different axes** and take different values. `[thinking].mode` is `off|on|auto` and controls deliberation; `[tui.activity].mode` is `default|detailed|debug` and controls timeline verbosity (`/view`). Changing one never changes the other.",
      "properties": {
        "deep_planning": {
          "default": true,
          "description": "Allow `on`/`auto` to promote a turn to grounded, staged execution.",
          "type": "boolean"
        },
        "minimum_duration_ms": {
          "default": 500,
          "description": "Anti-flicker floor: leave the activity component hidden until a turn has run at least this long, so sub-second turns never flash it. Zero disables the delay.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/ThinkingMode"
            }
          ],
          "default": "auto",
          "description": "Deliberation mode: `off`, `on`, or `auto`. This is the default for a fresh workspace; an explicit `/thinking` choice is stored in UI state and takes precedence from then on."
        },
        "summarize_provider_reasoning": {
          "default": true,
          "description": "Render provider-supplied reasoning *summaries* when a provider emits them. Raw provider reasoning is destroyed at ingestion and is never shown regardless of this setting.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ThinkingMode": {
      "description": "How much deliberation the harness performs, and how much of it is shown.",
      "oneOf": [
        {
          "description": "Optimize for latency. Optional deliberation is skipped for work that carries no risk flags, and the live activity component never renders.",
          "enum": [
            "off"
          ],
          "type": "string"
        },
        {
          "description": "Always render the activity component, and prefer grounded, staged execution with verification.",
          "enum": [
            "on"
          ],
          "type": "string"
        },
        {
          "description": "Decide per turn from the deterministic task classification. The recommended default.",
          "enum": [
            "auto"
          ],
          "type": "string"
        }
      ]
    },
    "TuiConfig": {
      "additionalProperties": false,
      "properties": {
        "activity": {
          "allOf": [
            {
              "$ref": "#/definitions/ActivityConfig"
            }
          ],
          "default": {
            "animation": "nexus",
            "animation_speed": "normal",
            "coalesce_events": true,
            "expand_shortcut": "ctrl+e",
            "mode": "default",
            "reasoning_preview_lines": 3,
            "reduced_motion": false,
            "show_diagnostics": false,
            "show_token_usage": false,
            "tool_icons": "geometric"
          },
          "description": "Live activity / inference timeline behavior (`[tui.activity]`)."
        }
      },
      "type": "object"
    },
    "WebConfig": {
      "additionalProperties": false,
      "properties": {
        "allow_loopback": {
          "default": false,
          "description": "Permit fetching loopback addresses (off by default; needed only for deliberate local-service testing).",
          "type": "boolean"
        },
        "allowlist": {
          "default": [],
          "description": "Hosts always allowed (exact or `*.suffix`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "denylist": {
          "default": [],
          "description": "Hosts always denied.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": true,
          "description": "Allow outbound web access at all.",
          "type": "boolean"
        },
        "max_fetch_bytes": {
          "default": 2000000,
          "description": "Maximum download size in bytes for page retrieval.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "per_host_delay_ms": {
          "default": 1000,
          "description": "Per-host minimum delay between requests, milliseconds.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "search_provider": {
          "default": "duckduckgo",
          "description": "Search provider: `duckduckgo` (no key required) or `none`.",
          "type": "string"
        },
        "timeout_secs": {
          "default": 30,
          "description": "Request timeout in seconds.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  "properties": {
    "general": {
      "allOf": [
        {
          "$ref": "#/definitions/GeneralConfig"
        }
      ],
      "default": {
        "default_agent": "nexus",
        "no_color": false,
        "reduced_motion": false,
        "test_command": null,
        "theme": "nexus-dark"
      }
    },
    "limits": {
      "allOf": [
        {
          "$ref": "#/definitions/LimitsConfig"
        }
      ],
      "default": {
        "completion_reserve_tokens": 1024,
        "context_compaction": {
          "enabled": true,
          "trigger_ratio": 0.75
        },
        "goal_runtime_budget_min": 120,
        "goal_step_budget": 200,
        "local_runaway_guard": {
          "enabled": true,
          "max_identical_tool_repeats": 3,
          "max_no_progress_cycles": 3,
          "max_weighted_tokens": null
        },
        "max_cost_micros_per_turn": 0,
        "max_failures_per_turn": 6,
        "max_memory_writes_per_turn": 8,
        "max_model_calls_per_turn": 24,
        "max_recursion_depth": 2,
        "max_repeated_calls": 3,
        "max_retries": 3,
        "max_steps_per_turn": 24,
        "max_subagents_per_run": 8,
        "max_tokens_per_turn": 250000,
        "max_tool_calls_per_turn": 48,
        "max_turn_runtime_min": 30,
        "retry": {
          "max_attempts": 3,
          "max_wait_seconds": 120
        },
        "self_hosted_context_window": 32768,
        "self_hosted_max_tokens_per_turn": 5000000
      }
    },
    "mcp": {
      "additionalProperties": {
        "$ref": "#/definitions/McpServerConfig"
      },
      "default": {},
      "description": "Registered MCP servers, e.g. `[mcp.my_server]`.",
      "type": "object"
    },
    "memory": {
      "allOf": [
        {
          "$ref": "#/definitions/MemoryConfig"
        }
      ],
      "default": {
        "default_ttl_days": 90,
        "enabled": true,
        "global_enabled": false
      }
    },
    "models": {
      "additionalProperties": {
        "$ref": "#/definitions/ModelConfig"
      },
      "default": {},
      "description": "Named model endpoints, e.g. `[models.local_main]`.",
      "type": "object"
    },
    "policy": {
      "allOf": [
        {
          "$ref": "#/definitions/PolicyConfig"
        }
      ],
      "default": {
        "allowed_commands": [],
        "commands": "ask",
        "denied_commands": [
          "sudo",
          "su",
          "doas",
          "shutdown",
          "reboot",
          "mkfs",
          "dd"
        ],
        "denied_paths": [],
        "destructive": "ask",
        "downloads": "ask",
        "external": "ask",
        "network": "allow",
        "read_formats": {},
        "reads": "allow",
        "writes": "ask"
      }
    },
    "routing": {
      "allOf": [
        {
          "$ref": "#/definitions/RoutingConfig"
        }
      ],
      "default": {
        "coding": null,
        "fallback": null,
        "planning": null,
        "simple": null
      }
    },
    "sandbox": {
      "allOf": [
        {
          "$ref": "#/definitions/SandboxConfig"
        }
      ],
      "default": {
        "backend": "auto",
        "container_image": "debian:bookworm-slim@sha256:7b140f374b289a7c2befc338f42ebe6441b7ea838a042bbd5acbfca6ec875818",
        "cpu_limit_secs": 120,
        "env_allowlist": [
          "PATH",
          "LANG",
          "TERM"
        ],
        "max_output_bytes": 200000,
        "memory_limit_mb": 1024,
        "network": "off",
        "timeout_secs": 120
      }
    },
    "self_improvement": {
      "allOf": [
        {
          "$ref": "#/definitions/SelfImprovementConfig"
        }
      ],
      "default": {
        "enabled": true,
        "surface_pending": true
      },
      "description": "Recursive Self-Improvement behavior (`[self_improvement]`): the flagship `nexus` agent's post-turn analysis that records approval-gated proposals."
    },
    "thinking": {
      "allOf": [
        {
          "$ref": "#/definitions/ThinkingConfig"
        }
      ],
      "default": {
        "deep_planning": true,
        "minimum_duration_ms": 500,
        "mode": "auto",
        "summarize_provider_reasoning": true
      },
      "description": "Deliberation behavior (`[thinking]`)."
    },
    "tui": {
      "allOf": [
        {
          "$ref": "#/definitions/TuiConfig"
        }
      ],
      "default": {
        "activity": {
          "animation": "nexus",
          "animation_speed": "normal",
          "coalesce_events": true,
          "expand_shortcut": "ctrl+e",
          "mode": "default",
          "reasoning_preview_lines": 3,
          "reduced_motion": false,
          "show_diagnostics": false,
          "show_token_usage": false,
          "tool_icons": "geometric"
        }
      },
      "description": "Terminal UI preferences, e.g. `[tui.activity]`."
    },
    "version": {
      "default": 0,
      "description": "Config schema version; migrated automatically when older.",
      "format": "uint32",
      "minimum": 0.0,
      "type": "integer"
    },
    "web": {
      "allOf": [
        {
          "$ref": "#/definitions/WebConfig"
        }
      ],
      "default": {
        "allow_loopback": false,
        "allowlist": [],
        "denylist": [],
        "enabled": true,
        "max_fetch_bytes": 2000000,
        "per_host_delay_ms": 1000,
        "search_provider": "duckduckgo",
        "timeout_secs": 30
      }
    }
  },
  "title": "Config",
  "type": "object"
}
